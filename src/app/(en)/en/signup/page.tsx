/**
 * English Signup Page — patient account creation (EN locale).
 * Renders the shared SignupClient component (i18n-driven).
 * Metadata is provided by the co-located layout.tsx.
 */
import SignupClient from "@/components/login/SignupClient";

export default function SignupRoute() {
  return <SignupClient />;
}