/**
 * Checkout page (FR) — shared checkout content via CheckoutView.
 */
import CheckoutView from "@/components/cart/CheckoutView";

export default function CheckoutPage() {
  return <CheckoutView />;
}